// Placeholder entry point for the SpanishType frontend.
// Replace this with your chosen framework bootstrap (React, Svelte, etc.).

document.addEventListener('DOMContentLoaded', () => {
  const root = document.getElementById('app');
  if (!root) return;

  const message = document.createElement('p');
  message.textContent = 'SpanishType frontend scaffolding goes here.';
  root.appendChild(message);
});
